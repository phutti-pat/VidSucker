{
    "configurations": [
        {
            "name": "Python: File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            // "justMyCode": true,
            "envFile": "${workspaceFolder}/env"
        }
    ]
}